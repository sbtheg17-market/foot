You are continuing an in-progress build from GitHub in a new Replit account.

Critical context:
- This is NOT a fresh start.
- The latest committed state on origin/main is the source of truth.
- The repo has already been built in checkpoint-sized chunks.
- Everything important should be discoverable from GitHub history and repo notes.

Top priority:
Keep the GitHub repo fully up to date and make sure the notes/commit history clearly show the build logic, sequence, and flow so the project is easy to resume later.

What the repo already has:
- working API server
- auth
- DB schema
- seed data
- provider-first mobile/web work
- booking state machine
- concurrency protections
- JSON error handling
- pressure tests
- push notification infrastructure
- route restructure toward /provider/*
- checkpoint commit strategy docs / notes
- all core changes pushed to origin/main

Your job:
1. Inspect the full repository before coding.
2. Read the existing notes and commit strategy docs.
3. Identify the latest checkpoint and what it changed.
4. Continue from the latest committed state only.
5. Make sure any new work is pushed to GitHub at checkpoint boundaries.
6. Keep commit messages and notes clear enough that the build flow is obvious later.
7. If you add or change any important architecture, document it in a repo note or session log.
8. Preserve the provider-first scope.
9. Do not add client/admin portals unless explicitly requested.
10. Do not add Stripe unless explicitly requested.
11. Do not rebuild already-working auth/schema/booking logic unless necessary.
12. Keep changes small, reviewable, and easy to merge.
13. Push stable chunks early so the repo never drifts too far from GitHub.

Important workflow rules:
- GitHub is the source of truth.
- Do not treat this Replit environment as the only place changes live.
- After each stable checkpoint, commit and push immediately.
- If the work is partially complete, still push the stable part and document what remains.
- Use notes/commit messages to explain why the change happened and how it fits the build sequence.
- Keep the build logical and sequential so the flow is obvious to future sessions.

Before coding, output:
1. What the repo currently contains
2. What was changed most recently
3. What should be continued next
4. What files/notes explain the build flow

Then proceed incrementally.