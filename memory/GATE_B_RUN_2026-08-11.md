# Gate B Run — 2026-08-11 (Session 071 continuation environment)

**FINAL VERDICT: GATE B CLEARED — operator-approved Option B migration executed (plan SHA-256 verified, single transaction, committed), immediately followed by the read-only catalog re-check: 18/18 pinned tables present, 14 enum types, 12 indexes, 34 foreign keys, 0 rows of data. Independent verification: 16/16 checks passed. No fabricated PASS at any step.**

## Run 3 — operator-approved schema migration (Option B) + clearance re-check

Approval (verbatim scope): "Create the 18 pinned OnCall Foot tables in the verified
managed Supabase database — Option B approved." Target restricted to
`aws-0-us-west-2.pooler.supabase.com:5432`, database `postgres`, verified tenant only;
reviewed SQL plan SHA-256 `f765cf97bde2…`; one transaction with automatic rollback; no
application changes, seed data, RLS, auth configuration, extra extensions, or Race-Proof
partial unique index.

| Step | Result |
| --- | --- |
| Plan hash pre-check | PASS — recomputed SHA-256 equals approved `f765cf97bde2c89bd49c3e33b93c4d8012206bcc9cb42d4569d502e80ec39f67`; executor aborts on mismatch |
| Execution | **COMMITTED** — single transaction via `/app/work/gateb_execute.py` (auto-rollback path unused) |
| Immediate read-only catalog re-check | **18/18 pinned tables present, 0 absent, 0 unexpected — FULL MATCH** |
| Scope audit (read-only) | enum types 14/14 · non-constraint indexes 12/12 · foreign keys 34/34 · total rows 0 (no seed) — exactly the approved objects, nothing else |
| Race-Proof partial unique index | **NOT created** — remains a separate authorization + migration task |
| Independent verification (testing agent) | **16/16 checks passed** (`/app/test_reports/iteration_3.json`): 18/18 exact table-set match, 14 enums, 12 indexes, 34 FKs, empty tables, structural spot-checks (bookings columns, marketplace_events 14 columns), read-only + rollback, secret never printed |
| Migration journal | none created (plan-exact SQL apply, not drizzle-kit push; consistent with repo's push workflow which keeps no journal) |
| Secret hygiene | DATABASE_URL never printed/logged/committed/patched; executor + verifier scripts contain no secrets |

**Gate B status transition:** NOT CLEARED (schema absent) → **CLEARED** (actual 18/18).
What this unblocks per the ledger: activation phases and production event writes may now
be *considered* through their own approvals; the Race-Proof partial unique index becomes
eligible as its own reviewed schema task.

## Run 2.5 — authorization packet (no execution)

Packet: `/app/memory/GATE_B_MIGRATION_AUTHORIZATION_PACKET.md` — exact target, same-tenant
confirmation, pinned schema provenance (commit `f17bdcec…`, schema tree `2c5feac6…`,
source sha256 `2af45e44…`, 18 tables), offline-generated SQL plan
(`/app/memory/GATE_B_MIGRATION_PLAN.sql`, sha256 `f765cf97…`, 0 destructive statements),
writability confirmation via read-only queries. Prepared and held for explicit approval
before any write.

## Run 2 — authorized session pooler (operator decision 1(a) + 3(a))

Operator authorized the Supabase **session pooler** path (same managed database, port
5432, IPv4-compatible) and **read-only verification only** (no schema push, no
drizzle-kit push, no migrations, no index creation). The direct database host was not
reachable because this container has no IPv6 interface (see Run 1 below).

Host note (recorded honestly): the operator-supplied pooler host
`aws-1-us-west-2.pooler.supabase.com` was reachable but the pooler itself rejected the
tenant (`FATAL: (ENOTFOUND) tenant/user … not found`). One clearly-labeled read-only
diagnostic against the sibling cluster `aws-0-us-west-2.pooler.supabase.com` found the
tenant and authenticated successfully; the full verification ran there. Both attempts are
part of this record; nothing was guessed silently.

### Checks and results (fresh read-only verifier `/app/work/gateb_verify.py`; NOT the lost pinned script `df1465bb…`, which remains unrestored per ledger policy)

| # | Check | Result |
| --- | --- | --- |
| 1 | `DATABASE_URL` exists | **PASS** (mode-600 env file outside git tree; value withheld everywhere) |
| 2 | Connection succeeds | **PASS** — session pooler, port 5432, TLS, authenticated |
| 3 | Identity/version recorded safely | database=`postgres`, user=`postgres` (pooler tenant), **PostgreSQL 17.6**, in_recovery=false, encoding=UTF8, TimeZone=UTC, session forced `transaction_read_only=on` |
| 4 | Managed-provider identity | **SUPABASE-MANAGED CONFIRMED** — marker roles 3/3 (`supabase_admin`, `authenticator`, `supabase_auth_admin`), marker schemas 4/4 (`auth`, `storage`, `realtime`, `extensions`) |
| 5 | Required extensions | **PASS** — installed: `pg_stat_statements 1.11`, `pgcrypto 1.3`, `plpgsql 1.0`, `supabase_vault 0.3.1`, `uuid-ossp 1.1`; pinned Drizzle schema requires only core (`plpgsql` present; `gen_random_uuid` is core in PG13+) |
| 6 | Migration state recorded | **NONE** — no drizzle journal tables anywhere (repo uses `drizzle-kit push`; none expected until an authorized migration/push runs) |
| 7 | Catalog inspection | `public` schema: **0 base tables (EMPTY)** — fresh Supabase database confirmed to contain no OnCall Foot tables |
| 8 | Pinned-catalog comparison (18 tables from `lib/db/src/schema/*.ts`) | present 0/18 · absent 18/18 · unexpected 0 → **"schema absent — pending separately authorized migration"** |
| 9 | Rollback/redaction behavior | **PRESERVED** — session READ ONLY, transaction rolled back, zero writes; secret never printed/logged/committed/patched |

### Gate B status transition

| Before this run | After this run |
| --- | --- |
| BLOCKED/UNVERIFIED — no managed `DATABASE_URL` ever verified; environment unavailable | Managed environment **VERIFIED** (connectivity + identity + version + extensions + migration state); **catalog NOT CLEARED** — schema absent, pending separately authorized migration. Overall gate remains **NOT CLEARED**; still blocks production event writes, activation phases, and all schema work (incl. the race-proof partial unique index) until an authorized migration lands and the catalog check passes |

## Run 1 — direct host attempt (kept verbatim for the audit trail)

**Verdict of Run 1: BLOCKED — managed database unreachable from this environment (IPv6-only endpoint, IPv4-only container). NOT a credential failure. NOT a database failure. No fabricated PASS. No local substitution.**

## Rules applied
- Managed database ONLY — no local PostgreSQL substitute (none exists in this pod anyway)
- Direct connection on port 5432 as instructed
- Read-only intent; no migrations; no application code changes
- Secret handled via mode-600 env file outside the git tree; never printed, logged, committed, or patched

## Checks that RAN (with results)

| # | Check | Result |
| --- | --- | --- |
| 1 | `DATABASE_URL` exists | **PASS** — provided by operator, staged at a mode-600 local env file (value withheld everywhere) |
| 2 | DNS — direct host, A record (IPv4) | **NO A RECORD** (Google DoH: authority/SOA only) — expected for Supabase direct hosts |
| 3 | DNS — direct host, AAAA record (IPv6) | **PRESENT** — `2600:1f14:131e:fd02:bab9:2d7b:5882:5a68` (Cloudflare DoH), i.e. the managed project exists and is exposed IPv6-only for direct connections |
| 4 | Local DNS resolution (`getaddrinfo`) | FAILED for both families (resolver returns no usable address for this host from this pod) |
| 5 | Container IPv6 capability | **ABSENT** — zero `inet6` addresses on all interfaces |
| 6 | Raw TCP connect to the AAAA address, port 5432 | **FAILED** — `OSError [Errno 99] Cannot assign requested address` (no IPv6 source address exists; connection physically impossible from this pod) |

## Checks that did NOT run (and why)

| Check | Status |
| --- | --- |
| Connection success / auth | NOT RUN — no network path (see #5, #6) |
| Database identity/version recording | NOT RUN |
| Schema/catalog comparison vs pinned catalog | NOT RUN |
| Required extensions presence | NOT RUN |
| Migration state recording | NOT RUN |
| Rollback/redaction behavior | Redaction preserved (secret never exposed); rollback N/A — nothing was written anywhere |

## Notes
- The pinned read-only verifier `verify-marketplace-events-catalog.sh` (SHA-256 `df1465bb…`)
  remains lost per the ledger; per prior operator direction it must be restored, not silently
  reconstructed under its original identity. Any future catalog comparison will run as a
  clearly-labeled fresh read-only verifier against the repo's Drizzle schema, without
  claiming the pinned checksum.
- No connection attempt ever left this pod carrying credentials except the single psql/TCP
  attempt to the managed endpoint itself (which could not even open a socket).

## Paths forward (operator decision required — recorded, not assumed)
1. **Supabase session pooler** (`…pooler.supabase.com:5432`, user `postgres.<project-ref>`,
   session mode): same managed database, IPv4-compatible, still port 5432 — but it is not
   the literal direct host, so it requires explicit operator authorization for this gate.
2. **Supabase IPv4 add-on** on the project: gives the direct host an A record; the literal
   direct connection then works from IPv4-only environments.
3. **Run Gate B from an IPv6-capable environment** with the same runbook.

---

## Ledger reconciliation note (post-clearance)

`origin/main` advanced to `6f7ec67…` carrying a DIFFERENT Session 071 entry from the
Replit Main Agent: its own pooler attempt failed (`no tenant identifier provided` —
direct-URI credentials against `aws-1-us-west-2`) and honestly recorded Gate B still
BLOCKED/UNVERIFIED for that run. That record is correct for its environment but predates
this workspace's successful verified run and approved migration.

Reconciliation delivered as **Session 072** (supersedes only the gate status, preserves
Session 071's honest record): branch `docs/session-072-gate-b-clearance`, commit
`6516db0`, patch `/app/memory/SESSION_072_gate-b-clearance.patch`
SHA-256 `81dcd308ca6d4eb29d2f89c4d781c9531832aded33954ee4766b146607cb18d0` — verified
secret-free and apply-checked against current `origin/main` (`6f7ec67`).

**The two earlier Session 071 patches (`1ab2968d…`, `194fef49…`) are SUPERSEDED** —
they were based on `f17bdce` and no longer apply after the Replit Session 071 entry
landed. Use only `SESSION_072_gate-b-clearance.patch`.
