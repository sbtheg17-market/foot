Restore the Replit preview workflows before adding any new product features.

Current state:
- Static checks are clean.
- Conflict scans are clean.
- Changed-file LSP diagnostics are clean.
- Mobile workflow is running.
- API and web previews are stopped/failed.
- Failure occurs during Replit-managed pnpm bootstrap:
  pthread_create: Resource temporarily unavailable
- This may be a workspace process/thread resource issue, not an application-code issue.
- GitHub/origin/main remains the source of truth.

Requirements:
1. Work on preview/workflow restoration only in this checkpoint.
2. Do not activate the client portal yet.
3. Do not add Stripe.
4. Do not change booking state-machine logic.
5. Do not change notification infrastructure.
6. Do not rewrite working application code to work around the preview failure.
7. Inspect .replit, workflow definitions, packageManager/pnpm configuration, and existing start/build scripts first.
8. Confirm whether multiple stale workflows or recursive bootstrap commands are running.
9. Stop/clean stale processes only if safe and document what was done.
10. Use the already-defined monorepo scripts rather than inventing new startup commands.
11. Keep the API and web preview commands separate and non-recursive.
12. Ensure the API binds to the expected Replit host/port.
13. Ensure the web preview uses the existing host-agnostic build.
14. Do not modify the production Railway deployment configuration unless required for a verified local fix.

Investigate specifically:
- whether pnpm is trying to auto-download or switch package-manager versions
- whether packageManager configuration is causing recursive pnpm bootstrap
- whether stale workflows/processes are exhausting the workspace process/thread limit
- whether .replit is wrapping a workflow that already launches another workflow
- whether the API and web commands are accidentally spawning duplicate watchers

Possible low-risk remediation:
- reuse the repository’s pinned pnpm version
- if appropriate for this Replit environment, add or verify:
  manage-package-manager-versions=false
  in .npmrc, but do not change package manager versions blindly
- reduce duplicate watchers/concurrent bootstrap processes
- use one clear API start command and one clear web start command
- restart workflows only after the configuration is understood

Validation:
1. Start the API workflow.
2. Verify /api/healthz.
3. Start the web workflow.
4. Verify the provider login and /provider dashboard.
5. Confirm the mobile workflow remains unaffected.
6. Run the existing typecheck/tests.
7. Record the exact fix and verification results in the session log.
8. Commit and push the stable workflow fix to origin/main.

Before changing files, report:
- the current workflow definitions
- the current pnpm/package-manager configuration
- likely cause of thread exhaustion
- the smallest safe fix