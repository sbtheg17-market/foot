Checkpoint d82a076 is complete and origin/main is synchronized.

The database recovery is verified:
- active development database target confirmed without exposing credentials
- existing Drizzle schema applied with pnpm run db:push
- pnpm run seed completed
- seed rerun successfully with no duplicates
- 5 demo users
- 2 linked provider profiles
- 5 services
- 4 sample bookings
- 1 review
- public provider discovery works
- API health works
- client login and /auth/me work in isolated verification
- unauthenticated booking access returns 401
- provider/admin booking creation returns 403
- client booking creation returns 201
- provider sees the booking
- client cancellation succeeds
- temporary verification booking was removed
- typecheck passes
- build passes
- all 63 booking state-machine tests pass
- web preview renders at 390px
- documentation checkpoint d82a076 is pushed and origin/main is synchronized

Only remaining blocker:
The managed API workflow does not receive JWT_SECRET, so managed login returns 500 even though health and public routes work.

Do this next and do not add product features:
1. Confirm the API reads process.env.JWT_SECRET.
2. Add a strong JWT_SECRET through the Replit Secrets panel for this project.
3. Do not place the value in source code, .env files, logs, or GitHub.
4. Restart the managed API workflow after adding the secret.
5. Verify managed login returns a signed JWT.
6. Verify /auth/me with the returned token.
7. Verify client, provider, and admin role redirects/guards.
8. Re-run API health, typecheck, build, and the 63 booking state-machine tests.
9. Confirm the web preview still loads /discover and /provider.
10. If no application files change, record the environment fix in the session log.
11. If a documentation-only change is made, commit and push it to origin/main.
12. Do not rotate or modify production/Railway secrets during this task.
13. Do not add Stripe or new client features until managed authentication is verified.

Never print or reveal the JWT_SECRET value.