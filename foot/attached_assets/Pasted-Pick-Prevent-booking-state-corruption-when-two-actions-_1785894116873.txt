Pick **“Prevent booking state corruption when two actions happen at once.”** That is the strongest next task because it protects the core booking state machine before you layer more alerts and UI polish on top. [perplexity](https://www.perplexity.ai/search/9c5bd666-4d4d-48ca-b753-309110401714)

## Why this first

If two actions happen at once, you can end up with conflicting transitions, duplicate updates, or stale UI. Locking down concurrency and state integrity first makes the cancel/reschedule alerts and unread badges much safer to build afterward.

## Next sequence for Replit

Paste this:

```md
Proceed with the next sequence in this order:

1. Prevent booking state corruption when two actions happen at once
2. Make providers aware when bookings are cancelled or rescheduled
3. Show unread booking alerts as badges so nothing gets missed

Important rules:
- Keep the booking state machine as the single source of truth.
- Guard against conflicting transitions and double-writes.
- Preserve the existing provider-first flows.
- Keep changes checkpoint-sized and pushable to GitHub frequently.
- After each stable chunk, commit and push immediately.
- Do not add Stripe yet.
- Do not add credential verification yet.
- Do not add new portals or monetization UI.
- If you need to refactor shared booking logic, do it once cleanly before adding feature-specific code.
- Keep mobile and web behavior consistent.

For step 1:
- add concurrency protection around booking state changes
- ensure simultaneous actions cannot corrupt status
- return clear errors or safe no-ops when a transition is invalid or stale
- keep booking flow reliable under repeated taps, refreshes, and overlapping requests

For step 2:
- send provider alerts for cancelled and rescheduled bookings
- reuse the existing push notification infrastructure
- ensure notifications do not break the booking flow

For step 3:
- surface unread booking alerts as badges in the provider UI
- make the badge count reflect actual unread booking events
- keep it lightweight and mobile-friendly

Goal:
stabilize booking integrity first, then layer alerts, then surface unread indicators, while keeping GitHub synced at every stable checkpoint.
```

## Why this order is best

This is the right order because:
- concurrency protection prevents data corruption,
- cancellation/reschedule alerts then ride on top of safe state transitions,
- unread badges are just the presentation layer on top of trusted events. [perplexity](https://www.perplexity.ai/search/9c5bd666-4d4d-48ca-b753-309110401714)

## Short version

If you want a shorter instruction:

```md
Do these in order: prevent booking state corruption from simultaneous actions, then add cancelled/rescheduled provider alerts, then show unread booking badges. Keep the booking state machine as source of truth and push checkpoint-sized changes to GitHub immediately after each stable chunk.
```

If you want, I can also turn this into a **super strict implementation checklist** for Replit so it knows exactly how to approach the concurrency fix.