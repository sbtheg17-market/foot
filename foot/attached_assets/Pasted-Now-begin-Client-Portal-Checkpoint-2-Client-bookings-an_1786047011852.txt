Now begin Client Portal Checkpoint 2: Client bookings and care history, but keep it focused on the booking lifecycle first. Do not add Stripe yet.

Send this to Replit or Emergent:

text
GitHub synchronization is complete.

Current source of truth:
- origin/main = cc1f995
- local HEAD = cc1f995
- ahead/behind = 0/0
- working tree is clean
- JWT managed-workflow verification passed
- API, web, mobile, and mockup workflows are healthy
- typecheck, build, and 63 booking state-machine tests pass

Begin the next product checkpoint:

Client Portal Checkpoint 2 — Client bookings and care history.

Build in this order:

1. Client bookings list
- upcoming bookings
- past bookings
- cancelled bookings
- clear status labels
- mobile-friendly layout

2. Client booking detail
- provider information
- service
- date and time
- address
- booking status
- relevant actions based on the existing state machine

3. Client cancellation
- only allow valid client cancellation transitions
- use the existing booking state machine
- require confirmation
- prevent duplicate submissions
- show clear success and error states

4. Client booking status visibility
- show updates after provider actions
- reuse existing notification and booking APIs
- do not duplicate booking-state logic in the client UI

5. Reviews
- allow a client to leave a review only after an eligible completed booking
- prevent duplicate reviews
- preserve the existing review model and API if already available

6. Care history
- add only the minimum client-visible care-history view needed for completed bookings
- do not expose provider-private notes unless the existing permission model explicitly allows it
- keep provider-private notes private

Scope boundaries:
- do not add Stripe
- do not add payments
- do not add admin portal UI
- do not change provider booking flows
- do not change the booking state machine unless a verified client transition is missing
- do not change database schema unless the existing client workflow truly requires it
- do not add unrelated design polish

Implementation rules:
- inspect the current client routes, mobile screens, API contracts, RBAC, and notification hooks first
- reuse existing booking endpoints and state machine
- keep web and mobile behavior consistent
- preserve public provider discovery and profiles
- maintain role-safe redirects and guards
- keep changes checkpoint-sized

GitHub rules:
- push every stable coherent chunk to origin/main
- do not wait for the entire checkpoint if a smaller slice is complete
- separate backend, web, and mobile changes when practical
- update .agents/LOG.md after each meaningful checkpoint
- show the commit hash after every push
- never commit secrets or environment values
- do not force-push or rewrite history

Before coding, report:
1. Current client routes and screens
2. Existing booking endpoints available to clients
3. Existing review and notification APIs
4. Any schema changes that appear necessary
5. The smallest first slice to implement

Stop after the first stable slice, verify it, push it, and report the commit hash.
Recommended first slice
Start with client upcoming/past/cancelled bookings and booking detail. That gives the client portal a usable core before adding cancellation, reviews, or care history.

The existing role guards and booking state machine should remain the source of truth; the client UI should only present valid actions and server responses. This avoids duplicating authorization or transition logic in the frontend.

Do not begin Stripe until the client booking lifecycle is working end to end.